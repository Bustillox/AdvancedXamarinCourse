<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="ExpensesApp.Views.ExpensesPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:ExpensesApp.ViewModels">
    <ContentPage.Resources>
        <ResourceDictionary>
            <vm:ExpensesVM x:Key="vm" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.ToolbarItems>
        <ToolbarItem Command="{Binding Source={StaticResource vm}, Path=ToAddExpenseCommand}" IconImageSource="add.png" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <ListView ItemsSource="{Binding Source={StaticResource vm}, Path=Expenses}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Detail="{Binding Category}" Text="{Binding Name}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>
